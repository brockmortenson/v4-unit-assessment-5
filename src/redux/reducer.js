import axios from 'axios';


const initialState = {
    username: null,
    profilePicture: null
};



export default function reducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}